

<div class="navbar">
  <div class="navbar-inner">
    <div class = "container">
      <a class="brand" href="/"> <%= image_tag "sslogo.png", :id => 'brandLogo', :class => 'brand'  %> </a>

    <ul class="nav pull-right">
  <% if user_signed_in? %>
    <li>
    Logged in as <%= current_user.name %>
    </li>
    <li>
    <%= link_to 'Logout', signout_path %>        
    </li>
  <% else %>
    <li>
    <%= link_to 'Login', signin_path %>  
    </li>
  <% end %>
</ul>
    </div>
  </div>
</div>

